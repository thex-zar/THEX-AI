name: build
on:
  push
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Force Python 3.10 and Fix SDK
        run: |
          sudo apt update
          sudo apt install -y aidl libffi-dev libssl-dev
          python -m pip install --upgrade pip==23.3.1
          pip install cython==0.29.36 buildozer==1.5.0
          # FORCE python3 to be 3.10 not 3.14
          sudo update-alternatives --install /usr/bin/python3 python3 /opt/hostedtoolcache/Python/3.10.21/x64/bin/python3 100
          sudo ln -sf /opt/hostedtoolcache/Python/3.10.21/x64/bin/python3 /usr/local/bin/python3
          sudo ln -sf /opt/hostedtoolcache/Python/3.10.21/x64/bin/python3 /usr/bin/python3.14 || true
          python3 --version
          SDK="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          yes | $SDK --licenses
          $SDK "build-tools;33.0.2" "platforms;android-33" "platform-tools" "ndk;25b"
          sudo cp /usr/bin/aidl $ANDROID_HOME/build-tools/33.0.2/aidl || true
          rm -rf ~/.buildozer
          mkdir -p ~/.buildozer/android/platform
          ln -s $ANDROID_HOME ~/.buildozer/android/platform/android-sdk
          mkdir -p $ANDROID_HOME/tools/bin
          echo '#!/bin/bash' > $ANDROID_HOME/tools/bin/sdkmanager
          echo 'exec $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "$@"' >> $ANDROID_HOME/tools/bin/sdkmanager
          chmod +x $ANDROID_HOME/tools/bin/sdkmanager
          # PRE-PATCH p4a to use old pip
          git clone https://github.com/kivy/python-for-android.git ~/.buildozer/android/platform/python-for-android
          cd ~/.buildozer/android/platform/python-for-android
          git checkout 2024.1.21
          sed -i 's/pip install -U pip/pip install pip==23.3.1/g' pythonforandroid/build.py
          grep -r "BuildDependencyInstallError" --include="*.py" . || echo "patched"
      - name: Build APK
        run: buildozer android debug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: THEX-APK
          path: bin/*.apk
